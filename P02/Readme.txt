futoshiki: 
	直接运行futoshiki_gac.exe即可，或编译futoshiki_gac.cpp（建议开O3优化）
blocksworld:
	1、运行前请先输入set_prolog_flag(answer_write_options, [max_depth(0)]).
	2、运行示例：
		start_state2(Start), end_state2(Goals), object_state2(Objects), my_plan(Start, Goals, Objects, Plan).
	这表明跑第二个样例。
